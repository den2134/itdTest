

{% block content_header '' %}

{% block content %}

    {%  if (form is defined) %}
        <form action="{{ url('post_article') }}" method="POST" {{ form_enctype(form) }}>

            <div>{{ form_widget(form) }}</div>

            <input type="submit" value="{% trans from 'ItdBundle' %}Submit{% endtrans %}">
        </form>
    {% endif %}

{% endblock %}

{% set code = code(_self) %}